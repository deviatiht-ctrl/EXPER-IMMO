/**
 * EXPERIMMO — config.js
 * Central configuration. Update API_URL after deploying backend to Render.
 */
const CONFIG = {
    API_URL: window.EXPER_API_URL || 'https://exper-immo.onrender.com',
};

export default CONFIG;
